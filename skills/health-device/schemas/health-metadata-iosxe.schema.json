{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "health-metadata-iosxe/v1",
  "title": "health/metadata-iosxe.json",
  "description": "IOS-XE visit pointer (Kind metadata). Health Device only. last_visit_id is the previous stamp. No port, no host, no credentials. RESTCONF port stays on inventory/prod.json. Not the five-field envelope.",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema", "updated_at", "source_agent", "iosxe"],
  "properties": {
    "schema": { "type": "string", "const": "health-metadata-iosxe/v1" },
    "updated_at": { "type": "string", "minLength": 1 },
    "source_agent": { "type": "string", "const": "health-device" },
    "iosxe": {
      "type": "object",
      "additionalProperties": false,
      "required": ["last_visit_id"],
      "properties": {
        "last_visit_id": {
          "type": ["string", "null"],
          "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}-[0-9]{2}-[0-9]{2}Z$",
          "description": "watch_id of the stamp this nurse wrote last. Next visit opens health/iosxe/<last_visit_id>.json."
        },
        "last_collected_at": { "type": ["string", "null"] }
      }
    }
  }
}
